package vooga.scroller.model;


public interface IInput {

    // We need to be able to replace a Input in our model, so we made this interface. We currently 
    // do not call any of these (or this) method. 
    
    
    
    /**
     * Is does this input class work? Perhaps.
     * @return
     */
    public boolean isValidInputClass();
    
    
  
}
